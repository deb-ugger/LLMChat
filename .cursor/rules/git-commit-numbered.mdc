---
description: Git commit messages use numbered Chinese bullet points
alwaysApply: true
---

# Git 提交说明格式

创建提交（用户要求 git 保存/提交）时，commit message 正文用编号分点，格式如下：

1. 第一点……
2. 第二点……
3. 第三点……

要求：
- 先查看上一提交，标题用一行概括本次改动
- 正文用 `1.` `2.` `3.` 逐项列出相对上一提交的具体差异（新功能、修复、行为变化等）；每个可独立验证的改动单独一点
- 不要用无序列表（`-`）作为正文主结构；需要时可在编号点内写短句
- 保持中文；详细写清楚“改了什么、为什么改、产生什么行为差异”，不要只写笼统套话
- 提交后立即复核最新的 commit message；如果本轮刚创建的提交没有按编号分点、没有与上一提交比较，或说明不够详细，使用 `git commit --amend` 立即修正
- 只修订本轮自己创建的最新提交；不得因此改写其他会话、其他人或已共享的历史提交
